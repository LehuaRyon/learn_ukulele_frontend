class Genre{
    constructor(name){
        this.id = id;
        this.name = name;
    }

    // instance method that renders genre object to dom

    // add a link from each genre name to show all songs that belong to that that genre
}